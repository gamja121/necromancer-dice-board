# AI 프로젝트 진행 상황 — 네크로멘서 앤드 다이스

## 작업 시작 전 필수 확인

- 사용자가 채팅에 첨부한 PNG/JPG 등 이미지를 GitHub에 추가·교체하는 작업은 **반드시 `GITHUB_IMAGE_UPLOAD_WORKFLOW.md`를 먼저 읽고 진행한다.**
- 직접적인 바이너리 업로드 도구를 찾지 못하더라도 중단하지 말고, 문서의 `create_blob → create_tree → create_commit → update_ref` 절차를 확인한다.
- 업로드 후에는 대상 경로와 최신 `main` commit을 다시 조회해 실제 반영 여부를 검증한다.

이 파일은 ChatGPT와 Codex가 현재 저장소의 작업 맥락을 함께 확인하기 위한 기록이다. 실제 구현 판단은 최신 코드와 `GAME_DESIGN_RULES.md`를 우선 확인한다.

## 2026-09-23: 카드 더미 버튼 → 주사위 컨트롤 카드

- 대상 화면: `v2-map-practice.html` (`v2-map-practice.css`, `v2-map-practice.js`).
- 맵 오른쪽 하단의 기존 카드 더미 버튼은 그대로 사용하되, **전장 덱 미리보기 연결을 제거**했다.
- 사용자가 제공한 “주사위 컨트롤 / 주사위 눈금 1이 나온다” 이미지를 카드만 남도록 투명 배경 PNG로 정리해 `art/v2-style/ui/dice-control-card.png`에 추가했다.
- 카드 더미 버튼을 누르면 이제 전장 덱 선택창이 아니라 **주사위 컨트롤 카드 단독 오버레이**가 중앙에 열린다.
- 배경을 누르거나 Esc를 누르면 닫힌다. 오버레이가 열린 동안 주사위 이동과 타일 재생성은 잠시 비활성화된다.
- 몬스터 타일 착지 시 4장을 선택해 전투로 들어가는 기존 전장 덱 선택 흐름은 그대로 유지된다.

## 이어서 할 일

- 실제 맵 화면에서 주사위 컨트롤 카드 표시 크기와 모바일 가로/세로 회전 시 배치를 육안 확인.
- 추후 주사위 제어 카드를 실제 보유/소비하는 시스템이 확정되면 이 오버레이를 보유 카드 목록과 연결한다.
