# AI 프로젝트 진행 상황 — 네크로멘서 앤드 다이스

## 작업 시작 전 필수 확인

- 사용자가 채팅에 첨부한 PNG/JPG 등 이미지를 GitHub에 추가·교체하는 작업은 **반드시 `GITHUB_IMAGE_UPLOAD_WORKFLOW.md`를 먼저 읽고 진행한다.**
- 직접적인 바이너리 업로드 도구를 찾지 못하더라도 중단하지 말고, 문서의 `create_blob → create_tree → create_commit → update_ref` 절차를 확인한다.
- 업로드 후에는 대상 경로와 최신 `main` commit을 다시 조회해 실제 반영 여부를 검증한다.

이 파일은 ChatGPT와 Codex가 현재 저장소의 작업 맥락을 함께 확인하기 위한 기록이다. 실제 구현 판단은 최신 코드와 `GAME_DESIGN_RULES.md`를 우선 확인한다.

## 2026-09-23: 카드 더미 버튼 → 주사위 컨트롤 카드

- 대상 화면: `v2-map-practice.html` (`v2-map-practice.css`, `v2-map-practice.js`).
- 맵 오른쪽 하단의 기존 카드 더미 버튼은 그대로 사용하되, **전장 덱 미리보기 연결을 제거**했다.
- 사용자가 제공한 “주사위 컨트롤 / 주사위 눈금 1이 나온다” 이미지를 카드만 남도록 투명 배경 PNG로 정리해 `art/v2-style/ui/dice-control-card.png`에 추가했다.
- 카드 더미 버튼을 누르면 이제 전장 덱 선택창이 아니라 **주사위 컨트롤 카드 단독 오버레이**가 중앙에 열린다.
- 배경을 누르거나 Esc를 누르면 닫힌다. 오버레이가 열린 동안 주사위 이동과 타일 재생성은 잠시 비활성화된다.
- 몬스터 타일 착지 시 4장을 선택해 전투로 들어가는 기존 전장 덱 선택 흐름은 그대로 유지된다.

## 이어서 할 일

- 실제 맵 화면에서 주사위 컨트롤 카드 표시 크기와 모바일 가로/세로 회전 시 배치를 육안 확인.
- 추후 주사위 제어 카드를 실제 보유/소비하는 시스템이 확정되면 이 오버레이를 보유 카드 목록과 연결한다.


## 2026-09-25: 주사위 컨트롤 카드 이미지 점검 및 작업 방식 보강

- `art/v2-style/ui/dice-control-*.png` 계열을 별도로 점검했다.
- 영문 `dice-control-exclude-1.png`, `dice-control-exclude-2.png`는 확장자는 PNG지만 실제 내부 포맷이 JPEG/JFIF인 상태가 확인됐다.
- 일부 카드에는 알파 채널이 없거나 카드별 해상도/용량 편차가 크다.
- 현재 `v2-map-practice.html`의 주사위 컨트롤 오버레이는 여전히 `dice-control-card.png` 한 장만 표시하며, 새 exclude/high/low/odd/even/repeat/echo 카드 목록과 실제 소비 로직은 아직 연결되지 않았다.
- `dice-overlay.js`의 기본 주사위 면은 여전히 `[1,2,3,4,5,6]` 기반이며 새 컨트롤 카드 효과가 직접 반영된 상태로 보지 않는다.
- 영문 exclude 1은 기존 이미지를 직접 확인한 뒤 새로 그리지 않고, **기존 카드 디자인을 유지한 채 바깥 검은 배경만 투명 처리하여 실제 RGBA PNG로 교체**했다.
- exclude 1 수정 커밋: `d64befeadde5cd3a67225b2016d0aa0d25120a71`.
- 이미지 작업 중 도구 호출 상한에 걸리는 문제가 확인되어, 이후에는 **한 파일 수정 → 검증 → 다음 파일** 순서로 진행한다.
- 바이너리 이미지 직접 가공이 필요하면 `GITHUB_IMAGE_UPLOAD_WORKFLOW.md`의 일회성 GitHub Actions + Pillow 절차를 우선 참고한다.
- 사용자가 기존 이미지 보존을 요구한 경우 **원본 확인 없이 새 이미지를 생성하지 않는다.**



## 2026-09-26: 게임 최상위 정의와 개발 일정 확정

- 게임의 최상위 장르 정의를 **로그라이크 덱빌딩 TRPG**로 확정했다.
- 핵심 축은 **로그라이크 원정 / 마물 덱빌딩 / TRPG 사건**의 세 가지다.
- 사건은 단순 랜덤 이벤트가 아니라 전투·덱빌딩과 동급의 핵심 시스템으로 본다.
- 수많은 사건을 여러 종류와 연속 분기로 구성해, 매 원정마다 다른 이야기와 다른 게임 방향을 경험하게 하는 것을 목표로 한다.
- 사건은 선택지, 조건, 플래그, 후속 사건, 보유 마물·군단·낙인 조건, 지역 및 세계 상태 변화를 포함할 수 있다.
- 2026년 10월 말까지 사건 시스템을 제외한 코어 게임 루프를 웹 V2에서 완성하는 것을 목표로 한다.
- 2026년 11~12월에는 사건 시스템 구현에 집중한다.
- 2026년 12월 이후 Unity 이관을 시작해 2027년 1월 완성본 마무리를 목표로 한다.
- 상세 정의와 완료 기준은 `PROJECT_VISION_ROADMAP.md`를 따른다.


## 2026-09-26: 순찰 세계관 · 오염도 · 보스 출현 구조 확정

- 플레이어는 특정 지역의 이상 징후와 마물 증가 원인을 조사하는 **순찰 임무**를 맡은 네크로맨서로 시작한다.
- 반복 순찰 중 사건과 전투를 통해 오염의 원인을 파악하고, 최종적으로 보스를 제거하는 것이 큰 진행 축이다.
- **오염도는 세계 진행도이자 위험/성장 시간 조절 시스템**으로 사용한다.
- 오염도가 높아질수록 맵의 마물 타일 양이 늘고, 마물 타일에서 조우하는 적의 수와 질도 상승한다.
- 높은 오염은 더 위험하지만 강한 마물과 성장 기회를 얻을 가능성도 높이는 방향으로 설계한다.
- 사건과 선택으로 오염도를 낮출 수 있으며, 플레이어는 오염도를 관리해 원하는 마물·낙인·군단 덱을 완성할 시간을 확보한다.
- **보스 타일은 바퀴 수가 아니라 오염도 조건에 따라 생성**한다.
- 필요하면 최종 보스는 오염도 외에 사건 태그, 단서, 특정 선택 결과를 추가 조건으로 사용할 수 있다.
- 사건 시스템의 기본 구조로 **사건 태그**를 사용해 과거 선택을 기억한다.
- 사건 판정 실패는 단순 실패로 끝내지 않고, 다른 사건 체인·적·추적자·지역 변화 등 새로운 이야기로 이어질 수 있게 한다.
- 상세 내용은 `PROJECT_VISION_ROADMAP.md`의 “순찰 임무와 오염 시스템”을 기준으로 한다.


## 2026-09-26: 반격 낙인 전투 연출 수정

- 확인 결과 `v2-rules.js`의 반격 피해 계산은 이미 정상 구현되어 있었다.
- 문제는 `v2-auto-battle-practice.js`에서 반격 피해만 즉시 적용되고, 피격자가 실제로 **공격 모션 → 공격자 피격 모션**을 수행하는 시각 연출이 빠져 있던 점이었다.
- 수정 내용:
  - 기존의 즉시 `showDamage(actor, outcome.counterDamage)` 표시를 제거.
  - 원 공격 모션이 끝난 뒤, 반격자가 실제 공격 모션을 재생하도록 연결.
  - 반격 타격 시 공격/피격 SFX를 재생.
  - 공격자에게 피격 모션과 피해 숫자를 표시.
  - 전투 메시지에 `반격!`과 반격 피해량을 표시.
  - 반격으로 공격자가 사망한 경우 기존 사망 처리 흐름이 그대로 이어지도록 유지.
- 전투 규칙 자체는 변경하지 않았다.
  - 반격은 기존대로 공격력 50% 올림, 최소 1 피해.
  - 반격에는 낙인/연격 재발동 없음.
  - 반격으로 다시 반격하지 않음.
- 구현 커밋: `38efddce8c041b262704440fed69fab557708ca6`
- Codex 확인 시 `v2-auto-battle-practice.js`의 `outcome.counterDamage > 0 && target.alive` 블록을 우선 검토할 것.


## 2026-09-26: 회복/자해 수치 표시 누락 보강

- 기존 초록 회복 숫자 시트 `art/v2-style/ui/healing-digits-sheet.jpg`와 `V2DamageDigits.renderHealing()`을 그대로 재사용했다. 새 숫자 이미지는 만들지 않았다.
- `v2-rules.js`에서 주사위 판정 시 발생하는 지원 효과를 UI가 알 수 있도록 이벤트를 기록하도록 보강했다.
  - 회복 낙인 축복: 실제 회복량을 `brand-healing` heal 이벤트로 기록.
  - 회복 낙인 저주: 실제 피해량을 `brand-healing-curse` damage 이벤트로 기록.
  - 소환 낙인 축복: 소환물 실제 회복량을 `brand-summon` heal 이벤트로 기록.
  - 소환 낙인 저주: 본체 실제 피해량을 `brand-summon-curse` damage 이벤트로 기록.
  - 흡혈 낙인 저주: 공격 결과에 `selfDamage`를 반환해 실제 자해 피해 숫자를 표시할 수 있게 했다.
  - 시체 포식 패시브: 처치 시 실제 회복량을 `passive-feast` heal 이벤트로 기록.
- `v2-auto-battle-practice.js`에서 위 이벤트를 받아 기존 숫자 렌더러로 표시한다.
  - heal 이벤트 → 기존 초록 `+숫자` 시트.
  - damage 이벤트 / 흡혈 저주 자해 → 기존 피해 `-숫자` 시트.
  - 시체 포식 회복도 기존 초록 회복 숫자로 표시.
- 언데드 군단 회복 표시는 기존 구현을 그대로 유지했다.
- 공격력 상승 자체(피의 원한, 소환 강화)는 체력 숫자가 아니라 능력치 버프 피드백 문제이므로 이번 수정 범위에서는 별도 숫자 연출을 추가하지 않았다.
- 규칙 이벤트 기록 커밋: `92c8ac42a8a2d5c63851b44978114e7e6fcf5e49`
- 전투 UI 연결 커밋: `ef417153431ef9da9c6861bf777d4710a546a9f3`
- Codex 확인 포인트: `v2-rules.js`의 `roll()` 지원 효과 이벤트와 `attack().selfDamage`, `v2-auto-battle-practice.js`의 `supportEvents` / `attackEvents` 처리.


## 2026-09-26: 회복/피해 숫자 연출 재검증 및 누락 보완

- 직전 회복/자해 수치 표시 작업 이후 `v2-rules.js`와 `v2-auto-battle-practice.js`를 다시 대조했다.
- 두 파일 모두 JavaScript 문법 검사를 통과했다.
- 기존 구현과 충돌 여부를 확인한 결과:
  - 언데드 군단 회복은 기존 `source: 'skeleton'` 경로를 그대로 사용하므로 새 `brand-*` 이벤트 처리와 중복되지 않는다.
  - 회복/소환 낙인 이벤트는 `brand-*`만 UI에서 별도로 처리하므로 기존 공격/중독/반격 숫자와 충돌하지 않는다.
  - 흡혈 축복은 기존 `outcome.recovered` 경로를 유지하고, 흡혈 저주만 `outcome.selfDamage`로 추가되어 중복 표시되지 않는다.
  - 공격 중 시체 포식 회복은 해당 공격 이후 새로 발생한 이벤트만 `attackEvents`로 잘라 처리하므로 이전 이벤트가 다시 표시되지 않는다.
- 재검증 중 **중독 지속 피해로 적을 처치했을 때 시체 포식 패시브 회복은 실제 적용되지만 초록 회복 숫자가 표시되지 않는 누락**을 추가로 발견했다.
- 이를 보완해 `V2Rules.before()` 호출 전 이벤트 인덱스를 저장하고, 중독 틱 처리 중 새로 발생한 `passive-feast` 회복 이벤트를 기존 `showHealing()`으로 표시하도록 연결했다.
- 보완 커밋: `03468d3edc513d28dba1de4b08478a6d653cd443`
- 현재 체력 변화 계열에서 확인된 표시 경로:
  - 일반 공격 피해 → 기존 피해 숫자
  - 중독 피해 → 기존 피해 숫자
  - 반격 피해 → 기존 피해 숫자
  - 언데드 군단 회복 → 기존 초록 회복 숫자
  - 회복 낙인 축복 → 기존 초록 회복 숫자
  - 회복 낙인 저주 → 기존 피해 숫자
  - 소환 낙인 축복 회복 → 기존 초록 회복 숫자
  - 소환 낙인 저주 → 기존 피해 숫자
  - 흡혈 축복 → 기존 초록 회복 숫자
  - 흡혈 저주 → 기존 피해 숫자
  - 시체 포식 패시브(직접 공격/중독 처치) → 기존 초록 회복 숫자
- 피의 원한 공격력 증가, 소환 강화 공격력 증가 등 **체력이 아닌 능력치 변화**는 별도 버프 피드백 설계 항목으로 남겨둔다.


## 2026-09-26: 중독 문구 시트 중복 표시 조건 수정

- 전투 상태 문구는 기존 이미지 시트를 그대로 사용한다.
  - `combat-labels-sheet.jpg`: 빗나감, 치명타
  - `status-labels-sheet.jpg`: 중독, 무적
- `V2DamageDigits.prepareLabels()`와 `prepareStatusLabels()` 로딩 경로가 정상 연결된 것을 재확인했다.
- 기존 중독 문구 조건은 대상이 이미 중독 상태이기만 해도 이후 공격에서 `중독` 문구가 다시 뜰 수 있었다.
- `v2-auto-battle-practice.js`에서 공격 전/후 `poisonStacks.length`를 비교하도록 수정했다.
- 이제 **이번 공격으로 중독 스택이 실제 증가한 경우에만** `V2DamageDigits.showLabel(target, "poison")`가 호출된다.
- 대상이 이미 중독 최대 스택이거나 중독 면역이라 스택이 늘지 않으면 중독 문구가 다시 뜨지 않는다.
- 첫 중독 부여 시 사용하는 `poisonAppliedTurn`도 실제 신규 스택이 들어온 경우에만 기록된다.
- 수정 커밋: `24bd5715d64b0cf47ed63de01447fa785d7c6df8`
- Codex 확인 포인트: `v2-auto-battle-practice.js`의 `poisonStacksBefore`, `poisonStacksAfter`, `poisonAppliedNow` 조건.


## 2026-09-26: 이미지 테스트 페이지에 현재 존재하는 주사위 컨트롤 카드만 연결

- Codex가 추가한 최신 주사위 컨트롤 카드 이미지 커밋 `11c9bf8a7cbd039332973b3f42c5c3a68192e3b2` 이후 `art/v2-style/ui/`의 실제 파일 목록을 다시 확인했다.
- `v2-image-test.html`이 존재하지 않는 카드까지 고정 목록으로 렌더링해 일부 항목에 `이미지 로드 실패`가 뜨는 구조를 수정했다.
- 이제 테스트 페이지는 현재 실제로 존재하는 언어 자산만 표시한다.
  - 숫자 1~5: 영문/한글
  - 숫자 6: 한글만
  - 효과 재발동, 큰수, 작은수, 홀수, 반복: 존재하는 영문/한글만 표시
  - 제외 1~6: 영문/한글
  - 짝수는 현재 파일이 없으므로 테스트 목록에서 제외
- `Echo` 표시는 기획 명칭에 맞춰 `효과 재발동 / Effect Reactivation`으로 표시한다.
- 새 이미지가 추가될 때는 `CARD_PAIRS`의 해당 항목에 언어 자산을 추가하면 된다.
- 수정 커밋: `b4cba68bc40f6dfcce4fe7eafa5c89b76d09858a`


## 2026-09-26: 이미지 테스트 페이지 최신 주사위 컨트롤 자산 반영

- `art/v2-style/ui/`의 현재 주사위 컨트롤 이미지 파일 목록을 다시 확인했다.
- 영문 `dice-control-6.png` 파일이 실제로 존재하지만 `v2-image-test.html`에서는 숫자 6을 한글만 표시하고 있던 누락을 수정했다.
- 숫자 6도 영문/한글 둘 다 표시하도록 변경했다.
- 같은 파일명으로 교체된 최신 카드가 브라우저 캐시에 가려지지 않도록 이미지 버전 쿼리를 `20260926-current-assets-2`로 갱신했다.
- 현재 파일이 존재하지 않는 Even 계열은 계속 테스트 목록에서 제외한다.
- 수정 커밋: `d6e6ab83a67243e5065071e556dbf95d20542138`


## 2026-09-26: 맵 주사위 자유 굴림 + 타일 벽 충돌 연출

- 기존 중앙의 검은 `dice-controller` 박스형 UI를 제거하고 주사위 자체만 보드 위에 보이도록 변경했다.
- 주사위를 누르면 기존 프레임 애니메이션을 사용하면서 맵 보드 위를 실제로 이동하도록 `animateMapDiceRoll()`을 추가했다.
- 외곽 타일의 실제 DOM 위치를 `getBoundingClientRect()`로 읽어 충돌 벽으로 사용한다.
- 주사위가 타일에 닿으면 충돌 법선 방향으로 속도를 반사시켜 튕겨나오며, 충돌 때 기존 `diceTick` 효과음을 재생한다.
- 맵 가장자리도 보조 벽으로 처리해 타일 사이 틈으로 주사위가 화면 밖으로 빠지는 것을 막는다.
- 이동 중 마찰을 적용해 후반부에 자연스럽게 감속하고, 멈출 때 실제 결과 이미지(`result-01~06.png`)로 확정한 뒤 기존 말 이동 로직을 그대로 이어간다.
- 타일 재생성 시 주사위 위치는 맵 중앙으로 초기화한다.
- 기존 주사위 결과 계산, 말 이동, 마물/워프/사건 처리 로직은 변경하지 않았다.
- CSS 커밋: `03df98dbd05b4d7125be270d2eb3e4790cb3279a`
- JS 커밋: `32bad0bb54cbb8c8fe4694f4cebea9b6001f3588`


## 2026-09-26: 맵 주사위 랜덤 물리감 강화

- 맵 주사위 굴림이 매번 같은 느낌이 나지 않도록 `animateMapDiceRoll()`의 물리 파라미터를 매 시도마다 랜덤화했다.
- 랜덤 대상:
  - 시작 방향
  - 초기 속도
  - 세로/가로 속도 비율
  - 목표 충돌 횟수(2~6회)
  - 전체 굴림 시간
  - 초기/후반 마찰 강도
  - 타일/보드 가장자리 반발력
  - 회전 방향
  - 회전 강도
  - 프레임 전환 속도
- 타일 충돌 시 아주 작은 접선 방향 랜덤 편차를 추가해 같은 각도로 반복 왕복하는 현상을 줄였다.
- 목표 충돌 횟수와 경과 시간에 따라 마찰이 점점 커지도록 해, 어떤 판은 짧고 묵직하게, 어떤 판은 더 오래 여러 번 튕긴 뒤 멈추도록 했다.
- 실제 주사위 결과값과 말 이동 규칙은 기존 그대로 유지한다.
- 수정 커밋: `707e056fe14243878e2bcd9a0f1df9af3a9802b3`


## 2026-09-26: 맵 주사위 랜덤 물리 강화 롤백

- 사용자 확인 결과 랜덤 물리 강화 버전이 부자연스러워 이전 동작으로 되돌렸다.
- `707e056fe14243878e2bcd9a0f1df9af3a9802b3`에서 추가한 랜덤 시작 속도/목표 충돌 횟수/마찰/반발력/회전 편차 등을 제거했다.
- `32bad0bb54cbb8c8fe4694f4cebea9b6001f3588` 시점의 주사위 동작으로 복원했다.
- 유지되는 기능: 박스 제거, 보드 위 자유 이동, 타일 벽 충돌, 타일/맵 가장자리 반사, 감속 후 결과 확정.
- 롤백 커밋: `79b1165a0e9009c8aae941e3df2221b3c3fc56a2`


## 2026-09-26: 맵 주사위는 강도만 랜덤화

- 이전 롤백 버전을 기준으로 시작 위치와 이동 방향 생성 방식은 그대로 유지했다.
- 굴림의 랜덤성은 투척 강도에만 적용했다.
- `throwStrength`를 매 굴림마다 0.48~0.92 범위에서 정해 약하게 굴러가는 경우와 세게 튀는 경우가 생기도록 했다.
- 굴림 지속시간은 1950ms로 고정해 시간/충돌횟수/반발력 등 다른 요소가 추가로 랜덤해지지 않도록 했다.
- 타일 충돌, 감속, 결과 확정, 말 이동 로직은 이전 승인 버전 그대로 유지한다.
- 수정 커밋: `e456742933ba8ba10e028165b91c0b0ab6aa6eee`


## 2026-09-26: 맵 주사위 시작점/타일 관통 수정

- 매 굴림마다 주사위가 이전 정지 위치에서 다시 시작하던 문제를 수정했다.
- 굴림 시작 직전에 `resetMapDicePosition()`을 호출하고 한 프레임 대기해 항상 승인된 중앙 시작점에서 출발하도록 했다.
- 강한 굴림에서 한 프레임 이동거리가 커 타일 충돌 판정을 건너뛰는(tunneling) 현상을 막기 위해 물리 이동을 최대 8개의 소단계로 나눴다.
- 각 소단계마다 맵 가장자리와 모든 타일 충돌을 다시 검사하므로 타일 위를 통과하는 현상을 줄였다.
- 기존 시작 방향 계산, 강도 랜덤, 반발/감속, 결과 확정, 말 이동 로직은 유지했다.
- 수정 커밋: `dfb58172dfbc8da9a9ce9d8c165fbc22dcdd73df`


## 2026-09-26: 맵 주사위 고정 시작점 보강

- 원인 확인: 굴림이 끝난 주사위가 마지막 정지 위치에 그대로 남아 있어 다음 클릭 전까지 임의 위치에 대기하고 있었다. 그래서 다음 굴림이 그 지점에서 시작하는 것처럼 보였다.
- 수정: 결과를 약 320ms 보여준 뒤 주사위를 항상 중앙 고정 시작점(`50%, 48%`)으로 복귀시킨다.
- 다음 굴림 직전에도 기존처럼 `resetMapDicePosition()`을 다시 호출하므로 시작점이 이중으로 보장된다.
- 타일 관통 방지용 소단계 충돌 판정은 그대로 유지한다.
- 수정 커밋: `4589cc36481c4b1389b9cd2323c6e54d10e7326c`


## 2026-09-26: 맵 주사위 전체 롤백 — 최초 보드 굴림 버전 복원

- 이후 추가했던 강도 랜덤화, 시작점 강제 복귀, 충돌 소단계 보강 등 맵 주사위 후속 수정들을 모두 제거했다.
- 기준 버전은 사용자가 처음 승인한 “박스 제거 + 보드 위 자유 굴림 + 타일을 벽처럼 충돌/반사 + 감속 후 결과 확정” 구현이다.
- JS는 최초 구현 커밋 `32bad0bb54cbb8c8fe4694f4cebea9b6001f3588`의 `v2-map-practice.js` 내용으로 정확히 복원했다.
- CSS는 최초 박스 제거 커밋 `03df98dbd05b4d7125be270d2eb3e4790cb3279a`의 `v2-map-practice.css` 기준을 확인했고 동일하지 않은 경우에만 복원했다.
- 복원 JS 커밋: `306c06cfe60f02ad945d65e3e2d60682e870317b`
- CSS는 이미 최초 버전과 동일해 추가 수정하지 않았다.



## 2026-09-26: 맵 타일 정보 UI 정리

- 현재 주사위 이동 로직은 그대로 유지했다.
- 맵 하단의 타일 정보 네모 상자(`.tile-info`)를 화면에서 숨겼다.
- 각 타일 우하단의 순번 숫자(`.map-tile .step`)를 화면에서 숨겼다.
- 타일 선택/이동/이벤트 판정 로직 자체는 변경하지 않았다.
- 수정 커밋: `ec93edb9cb1ba65e39b1a98df0c746b4f998f884`


## 2026-09-27: 집 나가기 시 한 바퀴 구름 전환 + 타일 재생성

- 현재 주사위 굴림 물리 로직은 수정하지 않았다. 최초 보드 굴림 기준 `32bad0bb54cbb8c8fe4694f4cebea9b6001f3588`의 `animateMapDiceRoll()`과 현재 코드가 동일함을 재검증했다.
- 사용자가 제공한 구름 원본에서 큰 구름 하나를 투명 PNG로 정리해 `art/v2-style/ui/map-cloud-transition.png`로 추가했다.
- 한 바퀴를 실제로 완료해 `HOME_INDEX`에 도착했을 때만 `lapReadyForRefresh = true`가 된다.
- 집 타일 장면의 **나가기 버튼**을 눌렀을 때만 구름 전환을 실행한다. ESC로 닫거나 시작 직후 집을 수동으로 여는 경우에는 타일을 바꾸지 않는다.
- 전환 순서:
  1. 집 장면 닫기
  2. 좌우의 큰 구름 6개가 화면 중앙으로 모여 맵을 가림
  3. 화면이 가려진 상태에서 `generateTiles()` 실행
  4. 구름이 다시 좌우로 빠짐
  5. 새 타일 맵에서 다음 바퀴 시작
- 구름 전환 중에는 주사위와 타일 재생성 입력을 잠근다.
- 이미지 커밋: `ad385536632c5b58c404a53f33f9f6572d81bc5a`
- HTML 커밋: `303d67ec94fefabce8b84c517b14de97abde13c6`
- CSS 커밋: `fbb2ed34ffc25c874e7c9d5a745a483ae34bbc38`
- JS 커밋: `84f09a55f6973b9bb705f6039af419e3647bbdc6`


## 2026-09-27: 오른쪽 주사위 컨트롤 카드 더미 5장 펼침 연출

- 오른쪽 하단 카드 더미를 누르면 기존 중앙 단일 카드 팝업 대신 카드 5장이 더미에서 왼쪽 방향으로 차례대로 펼쳐지도록 변경했다.
- 카드마다 약간 다른 회전각과 높이를 적용하고 55ms 간격으로 순차 등장시켜 셔플/패닝되는 느낌을 만들었다.
- 배경을 어둡게 덮던 기존 다이얼로그 연출은 제거하고 맵 위에 바로 카드가 펼쳐지도록 했다.
- 카드 더미는 펼친 상태에서도 오른쪽 하단에 유지되며 다시 누르거나 배경을 누르면 접힌다.
- 현재 시각 테스트용 5장은 실제 존재하는 한글 주사위 컨트롤 카드 `dice-control-ko-1.png` ~ `dice-control-ko-5.png`를 사용한다.
- 주사위 이동/충돌 물리 로직은 수정하지 않았다.
- HTML 커밋: `02835572845a5f2317a7b6de7d61a59774b0b6a1`
- CSS 커밋: `ee7c354fa4ad4609898d12d9466689377865f221`
- JS 커밋: `2e4ed91f919df32c2c477cb7e0013d44476a742c`


## 2026-09-27: 집 나가기 구름 전환 트리거 보강

- 구름 전환 코드와 CSS/HTML 연결은 그대로 존재하고 있었고, 최신 Pages 배포도 성공 상태였다.
- 실제 미동작 가능 지점은 `handleTileEventExit()`가 `lapReadyForRefresh` 임시 플래그까지 만족해야만 전환을 실행하던 조건이었다.
- 요구사항에 맞춰 집 장면의 **나가기 버튼 자체를 확정 트리거**로 변경했다.
- 이제 `activeEventTileId === "home"` 상태에서 나가기를 누르면 항상:
  1. 집 장면 닫기
  2. 구름 닫힘
  3. 완전히 가린 상태에서 타일 재생성
  4. 구름 열림
  순서가 실행된다.
- 주사위 이동/충돌 로직은 수정하지 않았다.
- 수정 커밋: `52e08123b6e2beb3188f57b200a548f183c3d0a0`


## 2026-09-27: 구름 전환 다양화 + 완전 화면 가림 보강

- 동일 구름 6장 반복 구조를 개선해 서로 다른 구름 자산 4종을 섞어 총 8개의 구름 레이어를 사용하도록 변경했다.
- 추가 자산: `map-cloud-transition-2.png`, `-3.png`, `-4.png`.
- 좌우 구름마다 크기, 높이, 등장 지연, 이동 시간을 다르게 설정해 서로 다른 속도로 중앙에서 만나도록 했다.
- 이동 시간은 약 620ms / 760ms / 900ms / 1020ms 네 단계로 섞었다.
- 구름 사이로 검은 맵 배경이 보이던 문제를 막기 위해 구름 아래에 밝은 안개 레이어를 두고, 구름이 만나는 시점에는 거의 완전히 불투명하게 차도록 했다.
- 타일 재생성은 모든 구름이 화면을 충분히 덮은 뒤 실행되도록 닫힘 대기 시간을 1200ms로 조정했다.
- 화면이 완전히 덮인 상태에서 타일을 재생성하고 잠시 유지한 뒤, 구름이 다시 서로 다른 속도로 좌우로 빠진다.
- 주사위 이동/충돌 물리 로직은 수정하지 않았다.
- 구름 자산 커밋: `3789b90c3ac39317cc580fe1976cec1e8b018d57`
- HTML 커밋: `5af24408caf674065b4498ebc76b20b167051787`
- CSS 커밋: `bde39f9fb48f979fb8d6f3901528d5ec7a192499`
- JS 커밋: `d9c64c8a567f28bcaae867a208e0218311133d10`


## 2026-09-27: 주사위 컨트롤 카드 위로 드래그해 사용

- 오른쪽 카드 더미에서 펼쳐진 주사위 컨트롤 카드를 터치/마우스로 잡아 위로 끌 수 있게 했다.
- 카드를 누르는 순간 선택 상태가 되고, 위로 약 46px 이상 당기면 사용 가능 표시가 나며 손을 놓는 순간 해당 카드가 사용 예약된다.
- 임계치보다 적게 당기고 놓으면 카드가 원래 자리로 되돌아간다.
- 사용된 카드는 5장 손패에서 제거되고, 기존 `pendingDiceControlId`에 등록되어 다음 주사위 굴림에 실제 효과가 적용된다.
- 이미 다음 굴림용 카드가 예약된 상태에서는 다른 카드를 추가로 소비하지 못하게 막았다.
- Repeat/Echo 등 기존 `V2DiceControl.canUse()` 사용 조건도 그대로 적용된다.
- 키보드 사용자는 Enter/Space로 같은 사용 동작을 할 수 있다.
- 카드 드래그 중에는 손가락 스크롤이 섞이지 않도록 `touch-action: none`을 적용했다.
- 주사위 이동/타일 충돌 물리 로직은 수정하지 않았다.
- JS 커밋: `e3bcec1508bfab9e2041e7c011b099815d3611e7`
- CSS 커밋: `707ef1e7cb60868d2389c3e712d9d5951f8c4c64`
- HTML 캐시 갱신 커밋: `ae187a08b84c0e5d4e0651c0ded11dcc148937e3`


## 2026-09-27: 주사위가 타일 링 밖으로 빠지는 문제 수정

- 증상: 모바일에서 드물게 주사위가 타일 바깥 영역으로 빠져서 맵 외곽에서 굴러다니는 장면이 발생했다.
- 원인: 기존 충돌은 각 타일의 개별 직사각형만 벽으로 사용하고 있었고, 타일과 타일 사이의 작은 틈은 실제로 열려 있었다. 주사위가 특정 각도/속도로 그 틈을 통과하면 링 밖으로 나갈 수 있었다.
- 수정: 기존 타일 충돌 로직은 유지하면서, 8개 상단 + 4개 오른쪽 + 7개 하단 + 5개 왼쪽 타일의 안쪽 모서리를 이용해 **연속된 보이지 않는 내부 경계**를 추가했다.
- 주사위의 방향, 속도, 감속, 회전, 결과 결정 로직은 변경하지 않았다.
- 카드 드래그/구름 전환 로직도 변경하지 않았다.
- 수정 커밋: `c5d1329c2b4b3ad21eb371aa9eb998a745433eb6`
