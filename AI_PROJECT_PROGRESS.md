# AI 프로젝트 진행 상황 — 네크로멘서 앤드 다이스

이 파일은 ChatGPT와 Codex가 현재 저장소의 작업 맥락을 함께 확인하기 위한 기록이다. 실제 구현 판단은 최신 코드와 `GAME_DESIGN_RULES.md`를 우선 확인한다.

## 2026-09-23: 맵 오른쪽 하단 카드 더미 버튼

- 대상 화면: `v2-map-practice.html` (`v2-map-practice.css`, `v2-map-practice.js`). `v2.html`의 **맵 테스트**에서 진입한다.
- 원본 녹색 배경 카드 더미 이미지를 참고하여 배경과 오른쪽의 흰 반짝임을 제거한 투명 PNG를 `art/v2-style/ui/map-card-deck.png`에 추가했다. 카드 더미 외에 버튼 프레임이나 메달 장식은 없다. 기존 참고용 JPG는 `art/v2-style/references/card-deck-reference.jpg`에 있다.
- 카드 더미 그림 자체를 맵 오른쪽 하단 클릭 버튼으로 사용한다. `mapCardDeckButton`을 누르면 기존 `mapDeckOverlay`의 마물 카드 선택 화면을 **미리보기**로 연다. 여기서 0~4장을 살펴볼 수 있지만 확인 버튼을 숨겨 전투에 진입하지 않는다. 닫기 버튼 또는 Esc로 돌아가며, 열린 동안 맵 이동과 타일 재생성을 잠시 막는다.
- 몬스터 타일에 착지하면 종전처럼 4장을 골라 확인 버튼으로 전투에 들어간다. 미리보기 선택은 저장되지 않으며 전투 덱 선택에도 영향을 주지 않는다. 보유 카드 상세 정보는 기존 왼쪽 하단 책 버튼에서 확인한다.
- 확인: `node --check v2-map-practice.js`, `git diff --check`, PNG 파일 형식 확인. 실제 브라우저 화면 및 모바일 실기기 배치는 아직 육안 확인이 필요하다.

## 이어서 할 일

- 실제 맵 화면에서 오른쪽 아래 버튼의 크기, 지도 타일과의 간섭, 가로/세로 회전 시 터치 영역 확인.
- 덱 편성 내용을 맵에서 저장하는 기능은 설계가 정해지면 별도로 구현. 현재 버튼은 카드 구성 미리보기다.
