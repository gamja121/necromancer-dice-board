# AI · GitHub 개발 운영 규칙

> **이 저장소에서 ChatGPT / Codex / 기타 AI가 작업을 시작하기 전에 반드시 읽는다.**
>
> 목표: GitHub API 호출 제한, ChatGPT 도구 호출 상한, 바이너리 이미지 처리 오류, 불필요한 Pages 재배포, 저장소 비대화와 같은 외부 문제를 줄여 개발을 끊기지 않게 한다.

## 0. 가장 중요한 한 줄

**읽기는 좁게, 검사는 작게, 수정은 원본 기준, 커밋은 묶어서, 실패하면 반복하지 말고 방식 전환, 완료 후 반드시 재검증한다.**

---

## 1. 작업 시작 전

1. 최신 `main` 기준으로 작업한다.
2. 이미 정확한 파일 경로를 알고 있으면 저장소 전체를 다시 스캔하지 않는다.
3. 작업 종류별 기준 문서를 먼저 확인한다.
   - 프로젝트 진행: `AI_PROJECT_PROGRESS.md`
   - 이미지 업로드/교체: `GITHUB_IMAGE_UPLOAD_WORKFLOW.md`
   - 게임 규칙: `GAME_DESIGN_RULES.md`
4. V1은 구현 기준이 아니다. V2 및 최신 문서를 우선한다.

---

## 2. GitHub/API 호출 규칙

### 기본 원칙

- 한 번의 AI 도구 실행에서 GitHub 파일을 대량 반복 호출하지 않는다.
- 실제 파일 내용 검사는 **1개씩**, 많아도 **2~4개 단위**로 나눈다.
- 저장소 전체 recursive tree 조회는 필요할 때만 한다.
- 한 번 tree에서 경로를 확보했으면 이후에는 정확한 path로 직접 접근한다.

### 실제로 확인된 오류

#### `Code Mode exceeded the maximum number of tool calls`

- GitHub 권한 오류가 아니다.
- 한 번의 실행에서 `fetch`, `fetch_file` 등을 너무 많이 반복했을 때 발생할 수 있다.
- 대응:
  1. 즉시 반복 호출을 줄인다.
  2. 작업 대상을 1~4개로 쪼갠다.
  3. 이미 얻은 경로·SHA·메타데이터를 재사용한다.

### 같은 실패 반복 금지

- 같은 방식이 **2회 연속 같은 이유로 실패하면 세 번째부터는 방법을 바꾼다.**
- 예:
  - binary를 text로 읽다가 실패 → base64 방식
  - 대량 fetch 실패 → 작은 묶음
  - 직접 이미지 교체가 어려움 → Git blob/tree/commit 또는 일회성 Actions

---

## 3. 텍스트와 이미지 처리 분리

### 텍스트 파일

대상:
- `.js`
- `.html`
- `.css`
- `.md`
- `.json`

원칙:
- 일반 `fetch_file` / 텍스트 update 방식 사용
- 수정 전 현재 SHA 확인
- 수정 후 새 SHA / commit 확인

### 이미지 파일

대상:
- `.png`
- `.jpg`
- `.jpeg`
- 기타 바이너리

원칙:
- 처음부터 바이너리로 취급한다.
- UTF-8 텍스트처럼 읽으려고 하지 않는다.
- 가능하면 `fetch_file(..., encoding="base64")` 형태로 확인한다.
- 확장자만 믿지 말고 실제 파일 시그니처도 확인한다.
  - PNG인지
  - JPEG/JFIF인지
  - 알파 채널이 실제 존재하는지

---

## 4. 기존 이미지 수정 규칙

사용자가 다음과 같이 말하면 **새 이미지를 생성하지 않는다.**

- "누끼만 따"
- "기존 그대로"
- "배경만 투명"
- "이 이미지 교체"
- "형식만 고쳐"
- "원본 유지"

반드시:

1. GitHub 기존 이미지를 먼저 실제로 확인한다.
2. 원본 디자인을 유지한다.
3. 필요한 부분만 가공한다.
4. 텍스트, 색, 구도, 프레임, 그림체를 임의로 재생성하지 않는다.

새 이미지 생성은 사용자가 명확하게 **새로 만들어 달라고 요청한 경우**에만 한다.

---

## 5. 이미지 업로드/교체 방법

일반 텍스트용 `update_file`로 바이너리를 억지로 넣지 않는다.

우선순위:

### A. Git data API 방식

`create_blob → create_tree → create_commit → update_ref`

사용 시:
- 이미지 추가
- 이미지 교체
- 여러 이미지를 한 commit에 묶기

### B. 일회성 GitHub Actions + Pillow

사용 시:
- GitHub에 있는 기존 이미지를 직접 가공해야 할 때
- 누끼
- 실제 PNG 변환
- 알파 추가
- 크기 최적화 등

규칙:
- standard runner만 사용
- artifact를 추가로 만들지 않는다.
- 작업 완료 후 임시 workflow 파일을 삭제한다.
- 원본을 먼저 확인한 뒤 처리한다.

### 실제 성공 사례

`art/v2-style/ui/dice-control-exclude-1.png`

- 기존 파일: 확장자는 PNG지만 실제 JPEG/JFIF
- 기존 카드 디자인 유지
- 바깥 검은 배경만 투명 처리
- 실제 RGBA PNG로 저장
- 수정 commit:
  `d64befeadde5cd3a67225b2016d0aa0d25120a71`

---

## 6. 검사와 커밋은 다르게 운영

### 검사/수정 준비

**한 파일씩** 또는 작은 묶음으로 처리한다.

이유:
- ChatGPT 도구 호출 상한 예방
- 오류 원인 추적 쉬움
- 잘못된 대량 수정 방지

### 최종 GitHub commit

관련 작업은 가능하면 **여러 파일을 한 commit으로 묶는다.**

예:
- 카드 6장 각각 검사/수정 준비
- 모두 확인 후 6장을 한 commit으로 업로드

이유:
- push 횟수 감소
- GitHub Pages 재배포 횟수 감소
- commit history 정리
- API 호출 감소

---

## 7. GitHub Pages / Actions 비용 및 부하 방지

현재 저장소는 public이며 Pages 배포가 연결되어 있다.

주의:
- push가 발생할 때 Pages 배포가 다시 실행될 수 있다.
- Pages artifact가 한 번에 약 200MB 이상 생성되는 사례가 실제 확인됐다.
- 이미지 1장마다 commit하면 불필요한 배포가 반복될 수 있다.

따라서:

1. 작은 이미지 수정은 가능하면 묶어서 commit한다.
2. 임시 workflow에서 `upload-artifact`를 사용하지 않는다.
3. `ubuntu-latest` 같은 standard runner만 사용한다.
4. larger runner는 사용하지 않는다.
5. 작업용 workflow는 완료 후 삭제한다.
6. LFS는 현재 사용하지 않는다.

---

## 8. 저장소 용량 관리

현재 이 프로젝트는 이미지가 많기 때문에 Git history가 쉽게 커질 수 있다.

규칙:

- 최종 게임에 필요 없는 중간 파일은 올리지 않는다.
- base64 운반용 txt를 저장소에 넣지 않는다.
- 임시 zip, 변환 파일, 테스트 출력물을 쌓지 않는다.
- 동일 이미지를 여러 폴더에 불필요하게 복제하지 않는다.
- 대용량 원본은 꼭 필요한 경우만 보관한다.
- 이미지 교체를 반복하면 과거 바이너리도 Git history에 남는다는 점을 기억한다.

---

## 9. 오류별 대응표

| 오류/상황 | 의미 | 대응 |
|---|---|---|
| `maximum number of tool calls` | AI 도구 호출 상한 | 작업을 1~4개 단위로 축소 |
| 401 | 인증 문제 가능성 | 연결/권한 확인 |
| 403 / 429 | GitHub rate/secondary limit 가능성 | 반복 호출 중단, 잠시 후 재시도 |
| UTF-8 / Unicode 오류 | 바이너리를 텍스트처럼 읽음 | base64 방식으로 전환 |
| 404 | 경로/branch 불일치 가능성 | path와 branch 재확인 |
| commit 성공했는데 화면 미반영 | 배포/캐시/잘못된 참조 가능성 | main HEAD, path, SHA, Pages 상태 확인 |
| 이미지 확장자와 내용 불일치 | 잘못된 저장 형식 | 실제 시그니처 확인 후 재인코딩 |
| 같은 오류 2회 반복 | 현재 접근 방식이 잘못됨 | 세 번째부터 방법 전환 |

---

## 10. 쓰기 작업 완료 조건

"성공" 응답만 보고 끝내지 않는다.

반드시 확인:

- 대상 파일이 실제 경로에 존재하는가
- `main` HEAD가 새 commit을 가리키는가
- 새 blob SHA가 반영됐는가
- 기존 파일을 실수로 삭제하지 않았는가
- 이미지라면:
  - 실제 포맷
  - 알파 여부
  - 해상도
  - 비율
  - 원본 디자인 유지 여부
- 임시 workflow / 중간 파일이 남지 않았는가

---

## 11. AI 작업 우선 행동 규칙

AI는 다음 행동을 우선한다.

1. 먼저 읽고 확인한다.
2. 이미 아는 정보를 다시 대량 조회하지 않는다.
3. 작은 범위로 수정한다.
4. 한 번에 과도한 도구 호출을 하지 않는다.
5. 실패 원인을 구분한다.
6. 같은 실패를 무작정 재시도하지 않는다.
7. 관련 수정은 한 commit으로 묶는다.
8. 완료 후 반드시 검증한다.
9. 사용자에게 실제 성공한 것만 "완료"라고 보고한다.

---

## 12. 작업 전 초간단 체크리스트

AI가 작업하기 전 아래 7개를 확인한다.

- [ ] 최신 main인가?
- [ ] 정확한 대상 path를 알고 있는가?
- [ ] 전체 tree를 다시 읽을 필요가 정말 있는가?
- [ ] 텍스트인가 바이너리인가?
- [ ] 기존 이미지 유지 요청인가?
- [ ] 호출을 작은 단위로 나눴는가?
- [ ] 최종 검증 방법을 정했는가?

---

## 관련 기준 문서

- `AI_PROJECT_PROGRESS.md`
- `GITHUB_IMAGE_UPLOAD_WORKFLOW.md`
- `GAME_DESIGN_RULES.md`
- `README.md`

이 문서는 **외부 개발 환경 운영 규칙의 최상위 기준**으로 사용한다.
